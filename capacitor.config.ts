import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "jp.animalpicturebook.app",
  appName: "Animal Picture Book",
  webDir: "dist"
};

export default config;
